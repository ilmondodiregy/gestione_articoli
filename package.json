// package.json
{
  "name": "magazzino-pro-cloud",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "predeploy": "npm run build", 
    "deploy": "gh-pages -d dist" 
  },
  "dependencies": {
    // ... omesso per brevità
  },
  "devDependencies": {
    // ... omesso per brevità
    "vite": "^6.2.0",
    "gh-pages": "^6.1.1" 
  }
}